name: Deploy
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: incubit-nl/infrastructure/.github/workflows/build-and-deploy.yaml@main
    with:
      app-name: laava-academy
